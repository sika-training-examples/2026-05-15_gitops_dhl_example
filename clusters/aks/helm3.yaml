apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dhl-example-helm3
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  project: default
  destination:
    namespace: dhl-example-helm3
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  source:
    repoURL: https://github.com/sika-training-examples/2026-05-13_dhl_example.git
    path: kubernetes/chart
    helm:
      valuesObject:
        host: helm3.aks.sikademo.com
        backend_EXTRA_TEXT_SUFFIX: helm1 v3